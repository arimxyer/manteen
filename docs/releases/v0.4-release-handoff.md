# Version 0.4 release handoff

[Documentation map](../project-context.md) · [Release receipts](README.md)

Status: **complete — the client-only `manteen@0.4.0` release is public and accepted.** Release PR
[#16](https://github.com/arimxyer/manteen/pull/16) merged as
`d93b24ac71942d939693994486db8b753d88e14b`; the signed `manteen-v0.4.0` tag, trusted release,
npm provenance and fresh controlled public-consumer lifecycle all passed.

## Release boundary

`0.4.0` is a client-only lifecycle release for explicit pruning of ordinary receipt-owned files
that their same current registry item no longer publishes. It adds only
`manteen remove --upstream-removed` with unselected discovery, exact repeated `--file` selection,
and the additional `--discard-adapted` intent required before deleting locally adapted source.

The command resolves every receipt item plus its current transitive dependency closure before it
grants deletion authority, never infers a rename, and journals the selected source, pristine base,
and receipt together. Its exact contract and evidence boundary are in
[`upstream-removal-handoff.md`](../handoffs/upstream-removal-handoff.md).

This release does not change `manteen-kit@0.2.0`, the client dependency on `manteen-kit@^0.2.0`,
receipt version 3, registry wire/schema behavior, update merging, verification, registry content,
or the Pages deployment. It creates no kit tag and requires no Pages dispatch.

## Accepted implementation baseline

PR [#15](https://github.com/arimxyer/manteen/pull/15) merged as
`2a4c94efdaf20e81676ce835e931f15bb7f6fdd3`. Its single hosted CI run
[`31207003141`](https://github.com/arimxyer/manteen/actions/runs/31207003141) passed source,
typecheck, lint, guards, Linux Node 22.12/24/26, macOS and Windows built execution, packed
npm/pnpm/Yarn/Bun consumers, the Windows packed npm lane, and the stable `CI gate`.

The final implementation pass reported 300 source tests, clean typecheck/lint/all five guards, a
22-item registry plus flat Node 22.12 CLI build, and 128 passing built-Node cases with one opt-in
package-manager smoke skipped. Independent review then closed parent symlink/junction containment,
relative registry-path rendering, and receipt-order preservation before merge.

Those implementation receipts proved the source and built bundle across the hosted matrix. They
did not prove the later tag, OIDC exchange, npm publication/provenance, public dependency
resolution, or a fresh lifecycle from the public tarball; the public receipt below closes those
separate boundaries.

## Candidate and publication sequence

1. Bump only `manteen` and the workspace lock entry to `0.4.0`; keep the kit at `0.2.0`.
2. Move the removal changelog under `0.4.0`, update the packed README/public CLI documentation,
   and pass the tag-specific release guard plus the complete local suite.
3. Merge one release-candidate PR after its required hosted matrix passes.
4. Create and push the signed `manteen-v0.4.0` tag at that exact merge commit.
5. Require the credential-free tagged release workflow to pass before reading npm as evidence.
6. Verify npm metadata, tarball integrity and SLSA provenance independently.
7. Install exact public `manteen@0.4.0` in a fresh disposable consumer and exercise unchanged and
   adapted upstream-removal transactions against controlled old/current registry revisions.

The public receipt will record the exact merge, tag, workflow, npm integrity/provenance and fresh
consumer facts only after they exist. A packed candidate, green PR or pushed tag is not
publication evidence.

## Local release-candidate receipt — 2026-08-08

From the release-candidate branch with `manteen` and its lock entry at `0.4.0`:

- `bun run test` passed 300 source tests;
- `bun run typecheck`, `bun run lint` and `bun run guard` passed, including all five repository
  guards;
- `bun run build:registry` emitted and validated all 22 items;
- `bun --cwd=packages/cli run build` produced the flat Node 22.12 bundle;
- `node packages/cli/scripts/run-e2e.mjs` passed 128 of 128 selected built-Node cases, with only
  the explicitly opt-in package-manager smoke skipped; and
- `node scripts/guard-release.mjs --tag manteen-v0.4.0` plus `git diff --check` passed against
  the inspected packed surface.

This was candidate evidence only. The later boundaries are recorded below rather than being
retroactively attributed to the local run.

## Hosted release and public acceptance — 2026-08-08

Release PR [#16](https://github.com/arimxyer/manteen/pull/16) carried candidate commit
`df3924e447d86840d42223b2becf01b4b5f76bca`. Its one CI run
[`31274763664`](https://github.com/arimxyer/manteen/actions/runs/31274763664) passed the source job,
Linux Node 22.12/24/26, macOS and Windows built execution, npm/pnpm/Yarn/Bun packed consumers, the
Windows packed npm lane, and `CI gate`. Marking the draft ready created no duplicate run. The PR
merged through branch protection as `d93b24ac71942d939693994486db8b753d88e14b`.

Before tagging, local `HEAD`, `origin/main` and that merge matched; the local/remote tag was absent;
npm returned 404 for `manteen@0.4.0`; and the tag-specific release guard passed again. The signed
tag verifies with Ari Mayer's ED25519 key and dereferences to the merge commit. Its tag object is
`55bfedb13eac126be20da0ddc60ee0beffe42325`.

The tag triggered exactly one trusted Release workflow,
[`31275076195`](https://github.com/arimxyer/manteen/actions/runs/31275076195). It passed the pinned
toolchain, source/built-Node/release guards and OIDC publish. npm then independently reported:

- `manteen@0.4.0` and `latest` at `0.4.0`, published `2026-08-08T19:44:03.862Z`;
- public dependency `manteen-kit@^0.2.0`, which resolved as `manteen-kit@0.2.0`;
- tarball `manteen-0.4.0.tgz`, SHA-1
  `03aabe6cc57e3980ab52c6d75b1b75b2ccc07230`, integrity
  `sha512-fYdK6TwvWai0TKY0nRHOjoi0l7duL9jxdgRuZVAWG9nkWnP30R3Qa98RuH1m21AzGsKE76wKwUKkX5kg7Pe2pA==`,
  12 files and 667,011 unpacked bytes; and
- both npm publish and SLSA provenance attestations.

The SLSA subject was `pkg:npm/manteen@0.4.0` with SHA-512
`7d874ae93c2f59a8b44ca6349d11ce8e88b497b76e2fd8f176046e6550161bd9e45a73f7d11dd06bdf11b87d66db50331ac284efac0ac142a45f9920ecf7b6a4`,
which is the npm integrity decoded to hex. It names `refs/tags/manteen-v0.4.0`, repository
`https://github.com/arimxyer/manteen`, `.github/workflows/release.yml`, merge commit
`d93b24ac71942d939693994486db8b753d88e14b`, and run `31275076195/attempts/1`.

A fresh disposable npm consumer then installed exact public `manteen@0.4.0`; `npm ls` resolved the
client from the registry tarball plus public `manteen-kit@0.2.0`, and the installed binary printed
`0.4.0`. The repository's controlled-registry removal harness ran against that public installed
bundle: help/usage plus the old-to-current lifecycle both passed (2/2). The lifecycle proved
unchanged discovery/preview/removal, adapted refusal then explicit discard, source/base/receipt
mutation, zero-file/direct receipt retention, unavailable/reassigned refusal and unrelated-byte
preservation. The local adaptation and registry revisions in that harness are controlled fixtures,
not observed consumer behavior or a claim about real-world removal frequency.

## Post-release follow-on

After public acceptance, the evidence-only AST-assisted merge-classification spike was completed
in [`ast-assisted-merge-spike.md`](../research/ast-assisted-merge-spike.md). It compares conservative
structural classification with the released line-oriented diff3 baseline without changing
`update`, `diff`, emitting AST-printed bytes, or claiming a conflict rate from synthetic consumer
adaptations. Any production integration still requires a separately frozen decision.
