{
  "name": "manteen",
  "version": "0.1.0",
  "description": "Install Mantine components from a registry into your project. The consumer side of manteen-kit.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "manteen": "./dist/cli.mjs"
  },
  "//exports": "schema/ holds three documents — the consumer config, the meta.mantine subschema and the install receipt — so the `./schema/*` pattern is needed alongside the bare `./schema`, which stays as the one a user puts in their $schema field. The comment lives out here because an `exports` object may not mix keys that start with `.` and keys that do not; a `//` key inside it is ERR_INVALID_PACKAGE_CONFIG.",
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "default": "./dist/index.mjs"
    },
    "./schema": "./schema/manteen.schema.json",
    "./schema/*": "./schema/*"
  },
  "files": [
    "dist",
    "schema",
    "README.md"
  ],
  "engines": {
    "node": ">=22.12"
  },
  "keywords": [
    "mantine",
    "registry",
    "components",
    "cli",
    "shadcn"
  ],
  "scripts": {
    "build": "tsdown",
    "test": "bun test",
    "//e2e": "The glob is required, not stylistic: node's --test directory discovery only picks up files matching its own name patterns (*.test.mjs, test-*.mjs, …), and `*.node-e2e.mjs` matches none of them. Passing the directory makes node 26 try to LOAD it as a module and fail with MODULE_NOT_FOUND, which reads like a broken test rather than a missed one.",
    "e2e": "node --test e2e/*.mjs",
    "//guard": "Only the guard this package owns. guard-runtime-apis is repo-wide, lives at the repo root, and resolves its scopes from there — running it with this package as cwd found nothing and exited 1, which is worse than not offering it. Use `bun run guard` at the root for both.",
    "guard": "node scripts/guard-diagnostics.mjs",
    "//prepare": "Same reason as the kit's: runs on install and on publish, so a fresh clone has dist/ and the package-name import resolves without a manual build.",
    "prepare": "tsdown"
  },
  "dependencies": {
    "@clack/prompts": "^1.7",
    "ajv": "^8",
    "commander": "^15",
    "diff": "^9",
    "get-tsconfig": "^4.14",
    "manteen-kit": "workspace:*",
    "nypm": "^0.6.9",
    "semver": "^7.8.5",
    "ts-morph": "^28.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/semver": "^7.7.1",
    "tsdown": "^0.22.14"
  }
}
